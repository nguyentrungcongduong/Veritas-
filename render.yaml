services:
  # 1. Backend Service (Laravel)
  - type: web
    name: veritas-backend
    runtime: docker # Changed from env to runtime
    dockerContext: backend
    dockerfilePath: Dockerfile.render
    envVars:
      - fromGroup: veritas-shared-secrets
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: DB_CONNECTION
        value: pgsql
      - key: CORS_ALLOWED_ORIGINS
        value: "*"

  # 2. Worker Service (Queues)
  - type: worker
    name: veritas-worker
    runtime: docker # Changed from env to runtime
    dockerContext: backend
    dockerfilePath: Dockerfile.render
    dockerCommand: "php artisan queue:work --tries=3"
    envVars:
      - fromGroup: veritas-shared-secrets
      - key: APP_ENV
        value: production
      - key: DB_CONNECTION
        value: pgsql

envGroups:
  - name: veritas-shared-secrets
    envVars:
      - key: DB_HOST
        sync: false
      - key: DB_PORT
        value: "5432" # Use string
      - key: DB_DATABASE
        sync: false
      - key: DB_USERNAME
        sync: false
      - key: DB_PASSWORD
        sync: false
      - key: APP_KEY
        generateValue: true
      - key: SESSION_DRIVER
        value: database
      - key: QUEUE_CONNECTION
        value: database
